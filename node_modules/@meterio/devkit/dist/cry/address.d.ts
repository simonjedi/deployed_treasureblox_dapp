/// <reference types="node" />
/**
 * derive Address from public key
 * @param pubKey the public key
 */
export declare function publicKeyToAddress(pubKey: Buffer): Buffer;
/**
 * to check if a value presents an address
 * @param v the value to be checked
 */
export declare function isAddress(v: any): v is string;
/**
 * encode the address to checksum address that is compatible with eip-55
 * @param address input address
 */
export declare function toChecksumAddress(address: string): string;
